<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>

        <PackageId>PMart.CoreMask.App</PackageId>
        <AssemblyName>$(PackageId)</AssemblyName>
        <RootNamespace>$(PackageId)</RootNamespace>

        <Title>$(PackageId)</Title>
        <Description>Library with tools related with core masks (selection of CPU cores).</Description>
        <PackageTags>tool;coremask</PackageTags>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="9.0.8"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\CoreMask\CoreMask.csproj"/>
    </ItemGroup>

</Project>
